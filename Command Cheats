--Sort dir from hihg to low---
du -h --max-depth=1 . | sort -rh


df, lsblk

sudo update-grub2



while ! yt-dlp -f 2 https://
do
    echo waiting for stream
    sleep 60
done

$ until cat missingfile
do
    echo waiting for missingfile
    sleep 10
done

$ until ./runme
do
    sleep 10
done
